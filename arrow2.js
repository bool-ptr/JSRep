const camera={
    price:600,
    weight: 2000,
    myDes: function(){
        return `this cannon is of ${this.price}`;
    }

}

console.log(camera.myDes());

// this is for redux people
//const func= ()=> ({key :'value'})
